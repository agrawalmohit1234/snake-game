export const NUM_ROWS = Math.ceil((window.innerWidth - 48) / 30);
export const NUM_COLS = Math.ceil((window.innerWidth - 48) / 30);

export const DIRECTION = {
  UP: "UP",
  DOWN: "DOWN",
  LEFT: "LEFT",
  RIGHT: "RIGHT",
};
